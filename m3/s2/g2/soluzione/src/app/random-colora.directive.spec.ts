import { RandomColoraDirective } from './random-colora.directive';

describe('RandomColoraDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomColoraDirective();
    expect(directive).toBeTruthy();
  });
});
